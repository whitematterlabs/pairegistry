name: grow-capability
kind: skill
topic: operating
version: 0.1.0
description: Handle a `request-capability:` IPC from a child PAI by scoping the need, executing claudecode to build it, and notifying the requester back. The "growth" half of the capability-gap escalation ceremony.
entrypoint: SKILL.md
deps:
  - execute-claudecode
