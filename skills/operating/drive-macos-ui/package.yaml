name: drive-macos-ui
kind: skill
topic: operating
version: 0.1.0
description: Drive a macOS app's UI when there's no CLI/API — prefer the ax driver (scoped AX sessions), fall back to osascript/System Events. Includes AX error-code decoding, inspect-act-readback verification, and fallback keyboard activation for Catalyst/SwiftUI controls.
entrypoint: SKILL.md
